#include <bits/stdc++.h>
using namespace std;
int n;
string s;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;

    while(n--){
        cin >> s;
        cout << s << "DORO" << " ";
    }
    return 0;
}